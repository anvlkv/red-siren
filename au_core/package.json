{
  "name": "au_core",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run wasm-build && vite",
    "build": "npm run wasm-build && tsc && vite build",
    "build:debug": "npm run wasm-build:debug && tsc && vite build ",
    "wasm-build:debug": "wasm-pack build --dev --no-default-features --features worklet",
    "wasm-build": "wasm-pack build --no-default-features --features worklet"
  },
  "author": "",
  "license": "CC0-1.0",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/audioworklet": "^0.0.54",
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3",
    "vite": "^5.1.3",
    "vite-plugin-wasm": "^3.3.0",
    "vite-plugin-top-level-await": "^1.4.1"
  }
}
