[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
mint = { workspace = true }
serde = { workspace = true }
tauri = { workspace = true, optional = true }
thiserror = { workspace = true }

[features]
tauri = ["dep:tauri"]
test = []
